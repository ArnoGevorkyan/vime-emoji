import { redirect } from "next/navigation";

export default function APHOPage() {
  // Immediately redirect to the external site
  redirect("https://same-sl94na0ykhd-latest.netlify.app/");
  // The following line is unreachable but keeps TypeScript satisfied
  return null as never;
} 